Sherlock Home Phase 5 documentation updater — v2

This version fixes the failed section matcher from the previous package.

It is designed against the public `main` repository state where:
- README.md preserves `docs/assets/sherlock-home_banner.png`
- README.md embeds `docs/assets/roadmap.svg`
- README.md still reports the pre-Phase-5 baseline
- docs/ROADMAP.md marks Phase 5 as NEXT
- docs/financial-tools.md is the Phase 5 design document
- docs/testing.md predates the final 234-test financial-analysis layer

The updater uses Markdown heading boundaries instead of matching entire section bodies.

Important behavior:
- Reads all target files first.
- Performs every transformation in memory.
- Validates banner/roadmap preservation and expected Phase 5/6 content.
- Writes files only after all transformations succeed.
- Does not modify Python application code, SQLAlchemy models, the database, or Alembic.

Apply from repository root:

    unzip -o sherlock-home-phase5-docs-update-v2.zip -d .
    python scripts/apply_phase5_docs_update_v2.py

Then:

    git diff --check
    git diff
    pytest -q

Expected test baseline:

    234 passed
